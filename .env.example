MONGO_URI=
DB_NAME=contesthub
JWT_SECRET=your_jwt_secret
PORT=5000
GOOGLE_APPLICATION_CREDENTIALS=./firebase-service-account.json
STRIPE_SECRET=
SITE_DOMAIN=http://localhost:5173
# Base64-encoded Firebase service account JSON (optional if using GOOGLE_APPLICATION_CREDENTIALS)
FB_SERVICE_KEY=
# Set this to your Firebase Storage bucket name (e.g. my-project.appspot.com)
FB_STORAGE_BUCKET=
